# Workflow разработки

## Общие правила

- Работа строго по плану из `tasklist.md`.
- Следовать принципам из `vision.md` и `conventions.md`.
- Каждая итерация должна быть завершена и протестирована перед переходом к следующей.

## Процесс выполнения итерации

### 1. Подготовка

- Прочитать описание итерации в `tasklist.md`.
- Изучить связанные требования из `vision.md`.
- Проверить текущий прогресс в таблице отчета.

### 2. Согласование решения

**Перед началом реализации:**

- Предложить предполагаемое решение с указанием:
  - какие файлы будут созданы/изменены;
  - какие функции/классы будут добавлены;
  - примеры кода (отрезки) для ключевых моментов.
- Дождаться согласования от пользователя.

### 3. Реализация

**После согласования:**

- Реализовать функционал согласно согласованному решению.
- Следовать правилам из `conventions.md`.
- Писать чистый, читаемый код.
- Добавлять минимально необходимые комментарии.

### 4. Подтверждение

- Предоставить краткое описание выполненной работы.
- Указать, что можно протестировать.
- Дождаться подтверждения от пользователя.

### 5. Обновление прогресса

**После подтверждения:**

- Отметить выполненные задачи в `tasklist.md` (чекбоксы `[x]`).
- Обновить статус итерации в таблице отчета:
  - `✅ Выполнено` — итерация завершена;
  - `🚧 В работе` — итерация в процессе;
  - `❌ Блокировано` — если есть блокирующие проблемы.
- Обновить процент прогресса итерации.
- При необходимости указать дату завершения.

### 6. Переход к следующей итерации

- Согласовать переход к следующей итерации.
- После согласования начать с шага 1 (Подготовка).

### 7. Коммит

- После завершения итерации (или нескольких связанных задач) сделать коммит.
- Сообщение коммита: краткое описание выполненной работы.
- Пример: `feat: реализована базовая структура проекта и команда /start`

## Исключения

- Если требуется отклониться от плана — согласовать с пользователем.
- Если обнаружена ошибка в предыдущей итерации — исправить перед продолжением.
- Если задача блокируется — обновить статус и согласовать дальнейшие действия.

## Приоритеты

1. Качество кода (читаемость, простота).
2. Соответствие требованиям из `vision.md`.
3. Тестируемость каждой итерации.
4. Документирование изменений.

