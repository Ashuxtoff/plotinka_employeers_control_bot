# Видение проекта

Разрабатываем Telegram-бота для архитектурного бюро.  
Цель — упростить контроль занятости сотрудников и автоматизировать ежедневные отметки о формате работы (офис, удалёнка, отпуск, отгул и т.д.) через интерфейс Telegram.

Бот будет полезен как руководителю (для оперативного контроля и аналитики), так и сотрудникам (для быстрой фиксации своего статуса без бюрократии и чатов).

---

# Основные сценарии

## 👤 Сотрудник

- Каждый день бот отправляет сообщение **в 08:00 по времени Екатеринбурга** с просьбой отметить формат работы.  

- Пользователь выбирает один из вариантов:

  - офис  

  - удалёнка  

  - учёба  

  - болезнь  

  - отпуск  

  - отгул неоплачиваемый  

  - отгул оплачиваемый  

  - экспедиция  

- Для отпусков, болезней и экспедиций бот запрашивает **диапазон дат** в формате `dd.MM.YYYY` или `dd.MM`.  

  - Если год не указан — используется текущий.  

  - Формат и последовательность дат валидируются.  

- Если пользователь не ответил до 15:00, бот присылает напоминание.  

- Если до 20:00 ответа нет — автоматически ставится статус **"отгул неопл."**  

- Если пользователь не найден в списке сотрудников (по user_id) — бот отвечает: **"Доступ закрыт."**  

- При первом запуске бот запрашивает **согласие на обработку персональных данных.**

---

## 🧑‍💼 Администратор (руководитель)

- По умолчанию один администратор — **@mirvien**.  

- Админ может:

  - Просматривать и выгружать сводку по сотрудникам (варианты периодов: **последние 7 дней**, **последние 30 дней**, **с начала месяца**).  

  - Исправлять данные сотрудников вручную:

    1. Выбрать сотрудника из списка.  

    2. Указать дату или диапазон дат.  

    3. Проверить формат даты (`dd.MM.YYYY` или `dd.MM`).  

    4. Выбрать значение из списка форматов работы.  

  - Добавлять новых сотрудников.  

  - Назначать других администраторов (ввести `@username`; бот проверяет, что он есть в списке сотрудников).  

  - Настраивать время рассылок (08:00 / 15:00 / 20:00).  

- При увольнении сотрудника его история **сохраняется**, но активность блокируется.

---

# 🕒 Рассылки и расписание

- Все рассылки выполняются по часовому поясу **Екатеринбурга (Asia/Yekaterinburg)**.  

- Администратор может менять время рассылок через меню «Настройки».  

- При перезапуске бот должен проверять, восстановлено ли расписание рассылок, чтобы избежать дублей.

---

# ⚙️ Технические детали

- Проверка **user_id** выполняется на каждом шаге.  

- Незарегистрированные пользователи не допускаются и не учитываются в статистике.  

- Формат даты — строго `dd.MM.YYYY` или `dd.MM`.  

- Хранение данных — в **SQLite** (используется и в MVP, и в продакшене).  

  - Причины выбора:

    - бесплатная и встроенная в Python;

    - не требует сервера или установки;

    - идеально подходит для небольшого количества сотрудников;

    - легко переносится при необходимости.

  - Таблицы:

    - `users(id, tg_id, username, name, role, active_flag)`  

    - `work_days(user_id, date, status, updated_at)`  

    - `vacations(user_id, start, end, type)`

- При необходимости база может быть перенесена в PostgreSQL без изменения структуры.

- Бот должен быть готов к **упаковке в `.exe` или Docker-образ**, чтобы заказчик мог запускать и останавливать его без IDE.  

- Все сообщения и ответы должны быть на русском языке.

---

# 🎯 Цель проекта

Создать минимально жизнеспособного Telegram-бота (MVP), который:

- работает полностью через Telegram,

- использует локальную базу SQLite (без серверной инфраструктуры),

- прост в установке и передаче заказчику,

- не требует IDE или дополнительных зависимостей,

- обеспечивает ежедневный контроль занятости сотрудников,

- даёт руководителю быстрый доступ к отчётам и управлению.

---

# 🔮 Возможности для будущего развития (за рамками MVP)

- Добавить LLM-агента для ответов на вопросы руководителя в свободной форме  

  *(например: "Кто сегодня на удалёнке?", "Сколько человек в офисе за неделю?")*.  

- Интеграция с Google Sheets или Notion для внешней отчётности.  

- Добавление графиков активности и аналитики.

---

LLM-ассистент в Cursor должен помогать:

- писать код бота на Python (используя `aiogram` или `python-telegram-bot`),

- проектировать и документировать структуры БД,

- создавать тесты и сценарии взаимодействия пользователей,

- формировать Dockerfile, `.exe` и скрипты запуска (`run.bat`, `start.sh`),

- поддерживать автогенерацию документации проекта по мере разработки.

